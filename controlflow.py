# For statement 
#syntax 
	# for x in range of i : 
		#do something 

w= [ 'cat', 'Dog', 'Mouse', 'Goat', 'Cow']
for i in w:
	print(i, len(i))


# Range function 
# Range allows to to iterate over a sequence of numbers especially in arithmetic progressions

for i in range(5):
	print(i)

# Range (a,b) is read as a <=x < b 
# Range (a,b,c) is read as a <= x < b in increments of c 


# Defining Functions

def (arg) :
	# statements 

#default argument values 
def ask_ok(prompt, retries =4, reminder="Please try again"):
	# statement

#Explanation 
# The function specifies a default value for the arguments retries and reminder. 
# Thus in calling the function, it is optional to give the optional arguments values. They 
# become optional arguments> 

# Functions arguments are evaluated at the point of the function definition in the defining scope. So careful how you do it.

 

# FUNCTION KEYWORD ARGUMENTS
# Functions can also be called using the keyword arguments of the form "kwarg=value". 

def parrot(voltage, state=' a stiff', action='voom' type=Kumerica Ash'):
	# statements 

# the function accepts one mandator argument(voltage) and three optional arguments 


